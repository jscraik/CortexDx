# FS, network, tool allowlists (stubs)
fs:
  allowedRoots:
    - apps/cortex-os
    - apps/cortex-cli
    - apps/cortex-os/packages
    - packages
    - libs
    - services
    - .cortexdx
network:
  egress:
    allow:
      - https://registry.npmjs.org
      - https://pypi.org
      - https://github.com
tools:
  mcp:
    allow: []
