{{- if .Values.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "insula-mcp.fullname" . }}-data-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insula-mcp.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size.data }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "insula-mcp.fullname" . }}-logs-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insula-mcp.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size.logs }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "insula-mcp.fullname" . }}-reports-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insula-mcp.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size.reports }}
{{- if or (eq .Values.tier "professional") (eq .Values.tier "enterprise") }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "insula-mcp.fullname" . }}-models-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insula-mcp.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size.models }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "insula-mcp.fullname" . }}-patterns-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insula-mcp.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: 10Gi
{{- end }}
{{- end }}
