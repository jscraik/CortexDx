apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cortexdx.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cortexdx.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
